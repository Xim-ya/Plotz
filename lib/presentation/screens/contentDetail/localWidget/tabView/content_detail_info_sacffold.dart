import 'package:soon_sak/utilities/index.dart';

class ContentDetailInfoScaffold extends StatelessWidget {
  const ContentDetailInfoScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
